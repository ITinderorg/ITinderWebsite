/** @type {import('next').NextConfig} */
const nextConfig = {
  swcMinify: true,
  /*i18n: {
    locales: ["ua"],
    defaultLocale: "ua",
    domains: [
      {
        domain: "itinder.com.ua",
        defaultLocale: "en-US",
        http: true,
      },
    ],
  },*/
};

module.exports = nextConfig;
